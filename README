Small utility for batching tasks for execution on remote machines.
Perl version

usage: spork [-t] [-f <filename>] [-u user] "remote command"
or: echo stdin: echo pclist | spork "remote command"

Defaults to running as same user on remote systems as is run as.
-t will run threaded, with the optional param the number of threads, or * to thread all
